name: PPR Registrations Historical CD

on:
  push:
    branches:
      - main
      - feature*
      - hotfix*
    paths:
      - "jobs/permanent/ppr-registrations-historical/**"
  workflow_dispatch:
    inputs:
      target:
        description: "Deploy To"
        required: true
        type: choice
        options:
        - dev
        - test
        - sandbox
        - prod

jobs:
  ppr-registrations-historical-cd:
    uses: bcgov/bcregistry-sre/.github/workflows/backend-job-cd.yaml@main
    with:
      target: ${{ inputs.target }}
      app_name: "ppr-reg-historical"
      working_directory: "./jobs/permanent/ppr-registrations-historical"
    secrets:
      WORKLOAD_IDENTIFY_POOLS_PROVIDER: ${{ secrets.WORKLOAD_IDENTIFY_POOLS_PROVIDER }}
      GCP_SERVICE_ACCOUNT: ${{ secrets.GCP_SERVICE_ACCOUNT }}
