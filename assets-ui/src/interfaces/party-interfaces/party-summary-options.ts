export interface PartySummaryOptionsI {
  header: string // can be '' if no header desired
  iconColor: string // can be '' if default colour desired
  iconImage: string // can be '' if no icon desired
  isDebtorSummary: boolean
  enableNoDataAction: boolean
  isRegisteringParty: boolean
}
