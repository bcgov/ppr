{
  "name": "ppr-git-hooks",
  "private": true,
  "type": "module",
  "description": "Root-level package for Git hooks tooling only - ppr-ui, ppr-api and mhr-api each have their own package/dependency management.",
  "scripts": {
    "prepare": "lefthook install || exit 0"
  },
  "devDependencies": {
    "lefthook": "^1.11.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "lefthook"
    ]
  }
}
