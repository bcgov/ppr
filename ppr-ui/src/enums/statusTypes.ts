export enum APIStatusTypes {
    DRAFT = 'D',
    ACTIVE = 'ACT',
    EXPIRED = 'HEX',
    DISCHARGED = 'HDC'
}

export enum UIStatusTypes {
    DRAFT = 'Draft',
    ACTIVE = 'Active',
    EXPIRED = 'Expired',
    DISCHARGED = 'Discharged'
}
